.tinyb
# Written by the Tinybird CD job to point `deploy` at a specific workspace host
# (the CLI has no --host flag); takes priority over the committed tinybird.json.
tinybird.config.json
.tinybird-generated
.tinybird-entities-*.mjs
# D1→Postgres migration dumps — contain prod data, never commit
.migration
**/.migration
.terraform
.DS_Store
.claude
.opencode
.env
.env.local
*.local

# mise per-developer overrides (machine-specific tool/env tweaks); mise.toml is committed
mise.local.toml

node_modules
.DS_Store
dist
dist-ssr
count.txt
.nitro
.tanstack
.wrangler
.output
.vinxi
.turbo
**/drizzle.config.local.ts
todos.json
apps/api/.data/
apps/api/scripts/.bench/

.alchemy
# eve agent build/model-catalog cache
.eve
.mcp.json
.dev.vars
apps/chat-agent/.dev.vars

# playwright (perf harness artifacts)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# sst
.sst
/bin/
/apps/agents/logs
/logs
/apps/ingest/apps/ingest/.wal
/apps/ingest/target
/apps/ingest/.wal

# local-only Drizzle Studio configuration
drizzle.config.local.ts
.18bd7*.bun-build
.[0-9a-f]*.bun-build
apps/ingest/dist/

# ios (apps/ios has its own .gitignore too; these guard the repo root)
apps/ios/Maple.xcodeproj/
apps/ios/Config/Secrets.xcconfig
apps/ios/build/

# Generic ClickHouse benchmark artifacts (may contain literal query inputs)
.bench/
