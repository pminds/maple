---
/**
 * HubPair — ComparePair for the hub: Maple on the left, the five vendors it
 * is compared against on the right, each mark linking to its page. Five don't
 * fit one row in the hero's aside, so the list wraps to 3 + 2, centred.
 */
import { MapleMark } from "@maple/ui/components/icons/maple-mark";
import BrandMarkIcon from "../BrandMarkIcon.astro";
import { comparePath, competitors } from "../../lib/competitors";
import { PairGrainient } from "./PairGrainient";

const locale = Astro.currentLocale ?? "en";
---

<div class="relative grid grid-cols-[1fr_1.6fr] gap-px overflow-hidden rounded-xl border border-border bg-border">
	<div class="pair-cell flex flex-col items-center gap-4 px-6 py-10 text-center md:py-12" style="--pair: var(--primary)">
		<PairGrainient client:only="react" color="#e8872a" />
		<span class="flex h-16 w-16 items-center justify-center rounded-xl border border-border bg-bg-elevated">
			<MapleMark size={34} className="text-primary" />
		</span>
		<div>
			<div class="font-display text-lg font-medium tracking-[-0.01em] text-fg md:text-xl">Maple</div>
			<div class="mt-1 font-mono text-[11px] text-fg-muted">maple.dev</div>
		</div>
	</div>
	<ul class="pair-cell flex flex-wrap content-center justify-center gap-x-3 gap-y-5 px-4 py-8 md:py-10" style="--pair: var(--muted-foreground)">
		{
			competitors.map((competitor) => (
				<li class="w-[4.75rem]">
					<a
						href={comparePath(locale, competitor.slug)}
						class="group flex flex-col items-center gap-2.5 text-center"
						aria-label={competitor.navLabel()}
					>
						<span class="flex h-11 w-11 items-center justify-center rounded-lg border border-border bg-bg-elevated transition-colors group-hover:border-fg-muted">
							<BrandMarkIcon id={competitor.mark} class="h-[22px] w-[22px]" />
						</span>
						<span class="whitespace-nowrap text-[11px] text-fg-muted transition-colors group-hover:text-fg">{competitor.name}</span>
					</a>
				</li>
			))
		}
	</ul>
	<span
		class="pointer-events-none absolute top-1/2 -translate-x-1/2 -translate-y-1/2 rounded-full border border-border bg-bg px-2.5 py-1 font-mono text-[10px] uppercase tracking-[0.14em] text-fg-muted"
		style="left: calc(100% / 2.6)"
		aria-hidden="true"
	>
		vs
	</span>
</div>
