// Moved to `@maple/query-engine` so the share API shapes `service_overview`
// rows exactly as the browser does. Re-exported here for the existing imports.
export { summarizeSampling } from "@maple/query-engine"
