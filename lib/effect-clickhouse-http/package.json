{
	"name": "@maple-dev/effect-clickhouse-http",
	"version": "0.1.0",
	"private": true,
	"description": "Effect-native, streaming ClickHouse HTTP queries for fetch runtimes",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc --noEmit",
		"test:live": "CLICKHOUSE_HTTP_LIVE=1 vitest run src/client.live.test.ts"
	},
	"devDependencies": {
		"@effect/language-service": "catalog:effect",
		"@effect/vitest": "catalog:effect",
		"@types/node": "catalog:tooling",
		"effect": "catalog:effect",
		"typescript": "catalog:tooling",
		"vitest": "catalog:"
	},
	"peerDependencies": {
		"effect": ">=4.0.0-rc.112 <5"
	}
}
